name: master-branch

on:
  push:
    branches:
      - 'master'

env:
  KEYSTORE_PASSWORD: ${{ secrets.ALIAS_PWD }}
  KEYSTORE_ALIAS: ${{ secrets.ALIAS }}
  KEYSTORE_KEY_PASSWORD: ${{ secrets.KEY_STORE_PASSWORD }}

jobs:
  build-signed:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11

      - name: Build release
        env:
          KEY_JKS: ${{ secrets.SIGNING_KEY }}
        run: |
          echo $KEY_JKS | base64 -d > app/keystore.jks
          ls
          ./gradlew assembleRelease \
            -Pandroid.injected.signing.store.file=$(pwd)/app/keystore.jks \
            -Pandroid.injected.signing.store.password=${{ secrets.KEY_STORE_PASSWORD }} \
            -Pandroid.injected.signing.key.alias=${{ secrets.ALIAS }} \
            -Pandroid.injected.signing.key.password=${{ secrets.ALIAS_PWD }} \

      - name: Copy artifacts
        id: artifacts_copy
        run: |
          mkdir artifacts
          cp ${{ steps.sign_apk_release.outputs.signedReleaseFile }} artifacts/app-release.apk
          echo "::set-output name=path::artifacts/"

      - name: Upload Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: artifacts
          path: ${{ steps.artifacts_copy.outputs.path }}